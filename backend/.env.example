# Server Configuration
PORT=5000

# Authentication Secrets
JWT_SECRET=ganpati-mandal-secret-key-12345

# Database Settings
# Note: SQLite database.sqlite is created automatically in the backend directory.
# For PostgreSQL transition in production, define the Postgres URI:
# DATABASE_URL=postgresql://user:password@localhost:5432/mandal_db
